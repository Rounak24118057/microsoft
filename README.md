Microsoft AI Unlocked
Hello there,
So this is the github repository for RedShield AI (more of it's basic version MediGuard)
MediGuard generates prompts to train the model dynamically, which creates a problem to upload all of that on the git repo, so what we did was we took a static version of MediGuard and uploaded it.
Everything's just the same in video, it's just static.

Hope, you liked it.

For any queries, pls mail at the leader's mail id.
